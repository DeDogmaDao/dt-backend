from django.apps import AppConfig


class BlockchainTransactionConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'project.blockchain_transaction'
    verbose_name = "BlockchainTransactions"
